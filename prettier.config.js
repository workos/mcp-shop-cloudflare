/**
 * @see https://prettier.io/docs/configuration
 * @type {import("prettier").Config}
 */
const config = {
  trailingComma: 'all',
  tabWidth: 2,
  semi: true,
  useTabs: false,
  singleQuote: true,
  printWidth: 120,
  arrowParens: 'avoid',
};

export default config;
